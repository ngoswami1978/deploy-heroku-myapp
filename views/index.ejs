<html>
<head> 
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Washing Your Cloth</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Lato:100" rel="stylesheet">
    <!-- <link href="/styles.css" rel="stylesheet"> -->
</head>

<body>
<div class="container">
<h2>
     Book Your Washing </h2> <h3><%= new Intl.DateTimeFormat('en-GB', { year: 'numeric', month: 'long', day: '2-digit'}).format(new Date()) %> </h3>

<form action ="/addtask" method="POST">
    
        <select class="selectpicker" data-style="btn-info" name="selectpicker">
            <% var options = []; %>
            <% for(var i = 0; i <= 11; i++) { %>
                <% if (i==0)   { %>
                    <option value='<%= i %>'><%= 'Select Room' %></option>
                    <% } else { %>             
                    <option value='<%= i %>'><%= 'Room ' + i %></option>
                    <% } %>
            <% } %>
        </select>       

        <input type="text" name="newtask" placeholder="add your name">
        <button> Add Task </button>

        <h2> Added Task date </h2>

        <% for( var i = 0; i < task.length; i++){ %>
            <li><input type="checkbox" name="check" value="<%= task[i] %>" /> <%= task[i] %> </li>
        <% } %>

        <button formaction="/bookedtask" type="submit" id="top"> Booked </button>
</form>

<h2> Booked Washing </h2>

<% for(var i = 0; i < errorMsg.length; i++){ %>
    <spam><%= errorMsg[i] %></spam>
<% } %>

<% for(var i = 0; i < complete.length; i++){ %>
    <li><input type="checkbox" checked><%= complete[i] %> </li>
<% } %>
</div>
</body>
</html>